server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/hrms_admin_db  # hrms_core_db
    username: postgres
    password: 260849
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/